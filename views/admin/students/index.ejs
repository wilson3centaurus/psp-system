<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Students | Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
</head>
<body>
  <div class="erp-shell">
    <aside class="erp-sidebar">
      <div class="sidebar-brand">
        <img src="/assets/logo.png" alt="Logo" class="logo">
        <div class="brand-text">Primary School Performance</div>
      </div>
      <nav class="sidebar-nav">
        <a class="sidebar-link" href="/admin/dashboard"><i class="fas fa-gauge"></i> Dashboard</a>
        <a class="sidebar-link" href="/admin/schools"><i class="fas fa-school"></i> Schools</a>
        <a class="sidebar-link" href="/admin/teachers"><i class="fas fa-users"></i> Teachers</a>
        <a class="sidebar-link active" href="/admin/students"><i class="fas fa-child"></i> Students</a>
        <a class="sidebar-link" href="/admin/resources"><i class="fas fa-boxes-stacked"></i> Resources</a>
        <a class="sidebar-link" href="/admin/attendance"><i class="fas fa-calendar-check"></i> Attendance</a>
        <a class="sidebar-link" href="/admin/analytics"><i class="fas fa-chart-pie"></i> Analytics</a>
        <a class="sidebar-link" href="/admin/reports"><i class="fas fa-file-alt"></i> Reports</a>
        <a class="sidebar-link" href="/itadmin"><i class="fas fa-user-cog"></i> IT Admins</a>
      </nav>
    </aside>

    <div class="erp-main">
      <header class="erp-topbar">
        <div class="title">Students</div>
        <div class="nav-actions">
          <a class="nav-link" href="/admin/dashboard"><i class="fas fa-home"></i> Dashboard</a>
          <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
      </header>

      <section class="erp-content">
        <div class="page-header">
          <div>
            <h2><i class="fas fa-user-graduate"></i> All Students</h2>
            <p class="subhead">Read-only view of every student across schools.</p>
          </div>
          <div class="card">
            <div class="metric">
              <i class="fas fa-users"></i>
              <div>
                <div class="label">Total Students</div>
                <div class="value"><%= totalStudents %></div>
              </div>
            </div>
          </div>
        </div>

        <% if (success_msg) { %>
          <div class="alert alert-success"><%= success_msg %></div>
        <% } %>
        <% if (error_msg) { %>
          <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>

        <% const queryString = `q=${encodeURIComponent(filters.search || '')}&schoolId=${encodeURIComponent(filters.schoolId || 'all')}&grade=${encodeURIComponent(filters.grade || 'all')}`; %>

        <div class="card">
          <form class="actions-row" method="GET" action="/admin/students">
            <input type="text" name="q" placeholder="Search name, class, ID..." value="<%= filters.search || '' %>" />

            <select name="schoolId">
              <option value="all" <%= filters.schoolId === 'all' ? 'selected' : '' %>>All Schools</option>
              <% schools.forEach(school => { %>
                <option value="<%= school.id %>" <%= String(filters.schoolId) === String(school.id) ? 'selected' : '' %>>
                  <%= school.username %>
                </option>
              <% }) %>
            </select>

            <select name="grade">
              <option value="all" <%= filters.grade === 'all' ? 'selected' : '' %>>All Grades</option>
              <% grades.forEach(g => { %>
                <option value="<%= g %>" <%= String(filters.grade) === String(g) ? 'selected' : '' %>><%= g %></option>
              <% }) %>
            </select>

            <button class="btn btn-primary" type="submit"><i class="fas fa-filter"></i> Apply</button>
            <a class="btn btn-outline" href="/admin/students/export/csv?<%= queryString %>"><i class="fas fa-file-csv"></i> Export CSV</a>
            <a class="btn btn-outline" href="/admin/students/export/pdf?<%= queryString %>"><i class="fas fa-file-pdf"></i> Export PDF</a>
          </form>
        </div>

        <div class="table-wrapper" style="margin-top: 1rem;">
          <table>
            <thead>
              <tr>
                <th>#</th>
                <th>Full Name</th>
                <th>Grade</th>
                <th>Class</th>
                <th>Gender</th>
                <th>Student ID</th>
                <th>School</th>
              </tr>
            </thead>
            <tbody>
              <% if (!students || students.length === 0) { %>
                <tr>
                  <td colspan="7">No students found.</td>
                </tr>
              <% } %>
              <% students.forEach((student, idx) => { %>
                <tr>
                  <td><%= idx + 1 %></td>
                  <td><%= student.name %></td>
                  <td><%= student.grade %></td>
                  <td><%= student.student_class %></td>
                  <td><%= student.gender %></td>
                  <td><%= student.student_id %></td>
                  <td>
                    <span class="badge">
                      <%= student.school_name || (`School #${student.school_id}`) %>
                    </span>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </section>

      <footer class="footer">
        2025 Primary School Performance System | powered by memory nikisi
      </footer>
    </div>
  </div>
</body>
</html>
