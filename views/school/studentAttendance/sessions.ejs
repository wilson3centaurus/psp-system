<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Attendance Sessions | School</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
</head>
<body>
  <div class="erp-shell">
    <aside class="erp-sidebar">
      <div class="sidebar-brand">
        <img src="/assets/logo.png" alt="Logo" class="logo">
        <div class="brand-text"><%= user.username %> (School)</div>
      </div>
      <nav class="sidebar-nav">
        <a class="sidebar-link" href="/school/dashboard"><i class="fas fa-gauge"></i> Dashboard</a>
        <a class="sidebar-link" href="/student"><i class="fas fa-user-graduate"></i> Students</a>
        <a class="sidebar-link" href="/teacher"><i class="fas fa-chalkboard-teacher"></i> Teachers</a>
        <a class="sidebar-link active" href="/student-attendance"><i class="fas fa-user-check"></i> Student Attendance</a>
        <a class="sidebar-link" href="/teacher-attendance"><i class="fas fa-user-clock"></i> Teacher Attendance</a>
        <a class="sidebar-link" href="/resources"><i class="fas fa-boxes-stacked"></i> Resources</a>
      </nav>
    </aside>

    <div class="erp-main">
      <header class="erp-topbar">
        <div class="title">Student Attendance Sessions</div>
        <div class="nav-actions">
          <a class="nav-link" href="/student-attendance/mark"><i class="fas fa-pen"></i> Mark</a>
          <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
      </header>

      <section class="erp-content">
        <div class="page-header">
          <div>
            <h2><i class="fas fa-calendar"></i> Attendance Sessions</h2>
            <p class="subhead">Browse recorded student attendance by date.</p>
          </div>
        </div>

        <% if (success_msg) { %>
          <div class="alert alert-success"><%= success_msg %></div>
        <% } %>
        <% if (error_msg) { %>
          <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>

        <div class="card">
          <form method="GET" action="/student-attendance" class="actions-row" style="flex-wrap: wrap;">
            <div style="flex:1; min-width: 200px;">
              <label>Date</label>
              <input type="date" name="date" value="<%= searchDate || '' %>">
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-filter"></i> Filter</button>
            <a href="/student-attendance" class="btn btn-outline"><i class="fas fa-rotate-left"></i> Reset</a>
            <a href="/student-attendance/mark" class="btn btn-secondary"><i class="fas fa-pen"></i> Mark Attendance</a>
          </form>
        </div>

        <div class="table-wrapper" style="margin-top: 1rem;">
          <table>
            <thead>
              <tr>
                <th>#</th>
                <th>Date</th>
                <th>View</th>
              </tr>
            </thead>
            <tbody>
              <% if (!sessions || sessions.length === 0) { %>
                <tr><td colspan="3">No sessions found.</td></tr>
              <% } else { %>
                <% sessions.forEach((session, idx) => { %>
                  <tr>
                    <td><%= idx + 1 %></td>
                    <td><%= session.date %></td>
                    <td><a class="btn btn-outline" href="/student-attendance/view/<%= session.date %>"><i class="fas fa-eye"></i> View</a></td>
                  </tr>
                <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </section>

      <footer class="footer">
        2025 Primary School Performance System | powered by memory nikisi
      </footer>
    </div>
  </div>
</body>
</html>
